// Intentionally removed. The previous MUI theme is unused and @mui/material
// is not a project dependency. Kept as an empty module to avoid breaking any
// stray imports during transition.
export {}