// The standalone TopBar component is no longer rendered by the application.
// The top bar (including the dataset selector) is now part of TrainingOverview
// and is covered by TrainingOverview.test.tsx.
describe('TopBar (removed)', () => {
  it('is no longer in use', () => {
    expect(true).toBe(true)
  })
})

export {}